<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment"> <fragment
    android:id="@+id/homeFragment"
    android:name="com.example.diariodebordo.HomeFragment"
    android:label="Diário"
    tools:layout="@layout/fragment_home">

    <action
        android:id="@+id/action_homeFragment_to_tripDetailFragment"
        app:destination="@id/tripDetailFragment" />
</fragment>

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.example.diariodebordo.MapFragment"
        android:label="Mapa"
        tools:layout="@layout/fragment_map" />

    <fragment
        android:id="@+id/galleryFragment"
        android:name="com.example.diariodebordo.GalleryFragment"
        android:label="Galeria"
        tools:layout="@layout/fragment_gallery" />

    <fragment
        android:id="@+id/tripDetailFragment"
        android:name="com.example.diariodebordo.TripDetailFragment"
        android:label="Detalhes da Viagem"
        tools:layout="@layout/fragment_trip_detail">

        <argument
            android:name="tripId"
            app:argType="integer"
            android:defaultValue="0" />
    </fragment>

</navigation>